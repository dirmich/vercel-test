{
  "name": "test",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun index.ts",
    "build": "bun-vercel ./index.ts",
    "build-old": "ls -la && bun build index.ts --compile --outfile=public/test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-vercel": "^1.0.0-alpha.5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "vercel-bun": "^0.0.1-alpha.5"
  }
}
